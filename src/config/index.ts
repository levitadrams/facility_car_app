/**
 * Exporta centralizada de todas as configurações do aplicativo.
 *
 * Uso recomendado:
 *   import { authConfig, apiConfig } from '@/config';
 */

export { authConfig, authStorageKeys } from './auth';
export { apiConfig } from './api';
export { biometricsConfig } from './biometrics';
